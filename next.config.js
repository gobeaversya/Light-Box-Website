/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing special needed — Next.js App Router handles everything.
  // Files in public/ are served statically, so uploads at public/uploads/
  // are accessible at /uploads/filename.jpg in the browser.
}

module.exports = nextConfig
